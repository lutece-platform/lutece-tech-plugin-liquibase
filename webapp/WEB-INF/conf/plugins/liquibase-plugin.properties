# {plugin.pluginName} properties file

#######################################################################################################
# Default Labels for XPage
liquibase.pageTitle=liquibase
liquibase.pagePathLabel=liquibase

#enable liquibase at application startup
liquibase.enabled.at.startup=false
#false to force liquibase starting without checking  if existing files not managed by liquibase
liquibase.safeRun=true

liquibase.first.run.request=select count(*) FROM information_schema.tables where table_name='DATABASECHANGELOG';
liquibase.empty.db.request=SELECT count(*) FROM information_schema.tables where table_schema=database();
# allow run liquibase on update file of unstable version at every startup
# the list of unstable versions are : alpha, beta, rc (release candidate)
liquibase.accept.unstable.versions=false
# allow run liquibase on update file of snapshot version at every startup
liquibase.accept.snapshot.versions=false
# enable dry run mode to output sql script instead of applying changes to the database
liquibase.dryrun=false
liquibase.dryrun.output.file=WEB-INF/plugins/liquibase/liquibase-dryrun.sql
# disable sending analytics to Liquibase
liquibase.analytics.enabled=false
# set log level for sql output (OFF, SEVERE, WARNING, INFO, CONFIG, FINE, FINER, FINEST, ALL, DEBUG)
liquibase.sql.logLevel=DEBUG
# enable migration mode to update plugin versions in datastore without applying liquibase changesets
liquibase.migration.mode=false

