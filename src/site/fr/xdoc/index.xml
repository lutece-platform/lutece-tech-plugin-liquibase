<?xml version="1.0" encoding="UTF-8"?>
<document>
	<properties>
		<title>Plugin liquibase</title>
	</properties>
	<head>
		<meta name="keywords" content="liquibase, sql" />
	</head>
	<body>
			<!--
            <p>
                <img src="images/liquibase.png"
			/>
            </p>
            -->
			<section name="Introduction">
				<p>This plugin allows for automatic execution of SQL scripts at application startup. Several modes are supported :
					<ul>
						<li>Creation : if the database is empty, all create and init scripts will be run.</li>
						<li>Migration : if this plugin was not in use in the previous version, the (previously non empty) database will be configured to use the plugin.</li>
						<li>Update : newly detected scripts will be run.</li>
					</ul>
				</p>
			</section>
			<section name="Configuration">
				<p>
					Par défaut, le plugin ne fait rien. Toutes les configurations sont définies dans le fichier <code>liquibase-plugin.properties</code>.
				</p>
				<subsection name="Propriétés générales">
					<table>
						<tr>
							<th>Propriété</th>
							<th>Description</th>
							<th>Valeur par défaut</th>
						</tr>
						<tr>
							<td><code>liquibase.enabled.at.startup</code></td>
							<td>Activer l'exécution du plugin au démarrage de l'application</td>
							<td>false</td>
						</tr>
						<tr>
							<td><code>liquibase.safeRun</code></td>
							<td>Forcer l'exécution de liquibase sans vérifier si les fichiers existants sont gérés par liquibase (false pour contourner la vérification de sécurité)</td>
							<td>true</td>
						</tr>
						<tr>
							<td><code>liquibase.first.run.request</code></td>
							<td>Requête SQL pour vérifier s'il s'agit de la première exécution (vérifie la table DATABASECHANGELOG)</td>
							<td>select count(*) FROM information_schema.tables where table_name='DATABASECHANGELOG';</td>
						</tr>
						<tr>
							<td><code>liquibase.empty.db.request</code></td>
							<td>Requête SQL pour vérifier si la base de données est vide</td>
							<td>SELECT count(*) FROM information_schema.tables where table_schema=database();</td>
						</tr>
					</table>
				</subsection>
				<subsection name="Contrôle de version">
					<table>
						<tr>
							<th>Propriété</th>
							<th>Description</th>
							<th>Valeur par défaut</th>
						</tr>
						<tr>
							<td><code>liquibase.accept.unstable.versions</code></td>
							<td>Autoriser l'exécution de liquibase sur les fichiers de mise à jour des versions instables (alpha, beta, rc) à chaque démarrage</td>
							<td>false</td>
						</tr>
						<tr>
							<td><code>liquibase.accept.snapshot.versions</code></td>
							<td>Autoriser l'exécution de liquibase sur les fichiers de mise à jour des versions snapshot à chaque démarrage</td>
							<td>false</td>
						</tr>
					</table>
				</subsection>
				<subsection name="Mode de simulation (Dry Run)">
					<table>
						<tr>
							<th>Propriété</th>
							<th>Description</th>
							<th>Valeur par défaut</th>
						</tr>
						<tr>
							<td><code>liquibase.dryrun</code></td>
							<td>Activer le mode de simulation pour générer un script SQL au lieu d'appliquer les modifications à la base de données</td>
							<td>false</td>
						</tr>
						<tr>
							<td><code>liquibase.dryrun.output.file</code></td>
							<td>Chemin du fichier de sortie pour le script SQL en mode simulation</td>
							<td>WEB-INF/plugins/liquibase/liquibase-dryrun.sql</td>
						</tr>
					</table>
				</subsection>
				<subsection name="Analytique et journalisation">
					<table>
						<tr>
							<th>Propriété</th>
							<th>Description</th>
							<th>Valeur par défaut</th>
						</tr>
						<tr>
							<td><code>liquibase.analytics.enabled</code></td>
							<td>Activer l'envoi d'analytiques à Liquibase</td>
							<td>false</td>
						</tr>
						<tr>
							<td><code>liquibase.sql.logLevel</code></td>
							<td>Définir le niveau de journalisation pour la sortie SQL (OFF, SEVERE, WARNING, INFO, CONFIG, FINE, FINER, FINEST, ALL, DEBUG)</td>
							<td>DEBUG</td>
						</tr>
					</table>
				</subsection>
				<subsection name="Mode de migration">
					<table>
						<tr>
							<th>Propriété</th>
							<th>Description</th>
							<th>Valeur par défaut</th>
						</tr>
						<tr>
							<td><code>liquibase.migration.mode</code></td>
							<td>Activer le mode de migration pour mettre à jour les versions du plugin dans le datastore sans appliquer les changesets de liquibase</td>
							<td>false</td>
						</tr>
					</table>
				</subsection>
				<subsection name="Gestion des erreurs">
					<table>
						<tr>
							<th>Propriété</th>
							<th>Description</th>
							<th>Valeur par défaut</th>
						</tr>
						<tr>
							<td><code>liquibase.failOnError</code></td>
							<td>Arrêter sur une erreur SQL dans un changeset (true) ou continuer avec les changesets suivants (false)</td>
							<td>true</td>
						</tr>
					</table>
				</subsection>
			</section>
			<section name="Ordre d'exécution : la directive d'en-tête runAfter">
				<p>
					Par défaut, liquibase exécute les fichiers SQL dans l'ordre alphabétique des chemins. Quand un
					plugin a besoin que ses scripts s'exécutent après ceux d'un autre plugin (typiquement pour insérer
					des lignes dans des tables créées par cet autre plugin), le contournement historique consistait à
					livrer le fichier SQL directement dans le répertoire de l'autre plugin. Cette pratique casse la
					résolution de version : l'inclusion du fichier est alors décidée par rapport à la version de
					l'autre plugin, jamais celle du propriétaire.
				</p>
				<p>
					La directive d'en-tête <code>runAfter</code> remplace cette pratique. Elle se déclare dans le bloc
					de commentaires de tête (avant le premier changeset) de n'importe quel script du plugin :
				</p>
				<pre>
--liquibase formatted sql
--lutece runAfter:genericattributes
--changeset forms:init_db_generic_attributes_forms.sql
INSERT INTO genatt_entry_type (id_type, title, ...) VALUES (1, 'Bouton radio', ...);
				</pre>
				<p>
					Tous les scripts du plugin déclarant (ici <code>forms</code>) sont alors ordonnés après tous les
					scripts du plugin cible (ici <code>genericattributes</code>), tout en restant physiquement dans le
					répertoire de leur propriétaire — la résolution de version reste donc correcte.
				</p>
				<ul>
					<li>La directive a une portée plugin : une seule déclaration dans un seul fichier suffit pour tout le plugin.</li>
					<li>Les directives se chaînent à n'importe quelle profondeur : si A déclare runAfter:B et B déclare runAfter:C, l'ordre résultant est C, puis B, puis A.</li>
					<li>Les directives invalides (cibles en conflit dans un même plugin, cible inconnue ou sans script, auto-référence, implication de core, cycles de dépendances) sont ignorées avec un log ERROR et le plugin garde sa position naturelle.</li>
				</ul>
			</section>
	</body>
</document>
